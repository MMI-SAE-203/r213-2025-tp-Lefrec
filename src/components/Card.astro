---
const {nomMaison,favori,imgURL} = Astro.props;
---

<div x-data=`{fav : ${favori}}` class="border p-4 rounded flex-initial min-w-[200px]">
    <img class="rounded-md" src={imgURL} alt="alt">
    <h3 class="text-2xl">{nomMaison}</h3>
    <button class="rounded-xl p-1 text-xs" :class="fav ? 'bg-blue-200' : 'bg-gray-200'" @click="fav =!fav">favori</button>
</div>